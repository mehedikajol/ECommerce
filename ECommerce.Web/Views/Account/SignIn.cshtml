@model SignInModel
@{
    Layout = "_AuthLayout";
    ViewData["Title"] = "Sign In";
}

<div class="container-fluid" style="margin-top: 120px;">
    <div class="rounded d-flex justify-content-center">
        <div class="col-lg-4 col-md-6 col-sm-12 shadow-lg p-5 pb-4 bg-light">
            <div class="text-center">
                <h3 class="text-primary">Sign In</h3>
            </div>
            <form class="form-horizontal" method="post" asp-controller="Account" asp-action="SignIn" asp-antiforgery="true">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <input type="hidden" asp-for="ReturnUrl" />
                <div class="p-4">
                    <div class="mb-3">
                        <div class="input-group">
                            <span class="input-group-text bg-primary">
                                <i class="fas fa-envelope"></i>
                            </span>
                            <input asp-for="Email" class="form-control" placeholder="Enter email">
                        </div>
                        <span asp-validation-for="Email" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <div class="input-group">
                            <span class="input-group-text bg-primary">
                                <i class="fas fa-key"></i>
                            </span>
                            <input asp-for="Password" class="form-control" placeholder="Enter password">
                        </div>
                        <span asp-validation-for="Password" class="text-danger"></span>
                    </div>

                    <div class="form-group row">
                        <div class="col-sm-10">
                            <div class="form-check">
                                <input type="checkbox" asp-for="RememberMe" class="form-check-input">
                                <label class="form-check-label" asp-for="RememberMe">Remember me</label>
                            </div>
                        </div>
                    </div>

                    <button class="btn btn-primary text-center" type="submit">
                        Sign In
                    </button>
                    <p class="text-center mt-5">
                        Don't have an account?
                        <a asp-action="Register" asp-controller="Account" asp-route-returnUrl="@Model.ReturnUrl" class="text-primary">Register</a>
                    </p>
                    <a asp-action="ForgetPassword" asp-controller="Account" class="text-primary text-center d-block">Forgot your password?</a>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}